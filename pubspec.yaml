name: ebook_malay__novel
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.0.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  supabase_flutter: ^2.5.0
  uuid: ^4.0.0
  url_launcher: ^6.3.2
  cached_network_image: ^3.3.0
  flutter_pdfview: ^1.4.1+1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true